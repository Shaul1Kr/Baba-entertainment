# PROMPTS.md

A chronological log of the prompts given during this build and a one-line summary of what was done in response to each.

| # | Prompt (summary) | Response (what was done) |
|---|------------------|--------------------------|
| 1 | Build a flash-sale store take-home: Node/Express/TS + MongoDB + Redis + Angular, DDD backend, Redis Lua atomic stock, event-driven reservation expiry, Socket.io live stock, Vegas-style Angular UI, Docker infra. Give a numbered plan first and confirm before Phase 1. | Produced a 5-phase numbered plan, stated tech defaults (tsx, Express 4, Angular standalone, Bearer=userId), and asked for confirmation. |
| 2 | Confirmed the plan ("Yes, start Phase 1"). | Scaffolded repo: docker-compose (mongo + redis w/ keyspace notifications), .env.example, backend TS project, domain entities + repository interfaces, Mongoose implementations, Redis client + Lua reserve script + StockReservationService + reservation-expiry subscriber. |
| 3 | (continued) Phase 2 + verification. | Built all use cases (Login/List/Add/Remove/GetCart/Checkout/ExpireReservation), thin Express controllers + routes + bearer auth + error handler, Socket.io broadcaster, composition root, server bootstrap with reconciling stock seed, and a catalogue seed script. Brought up Docker infra, typechecked, and end-to-end tested: 20 concurrent adds on stock-3 item → exactly 3 succeed (no oversell), remove restores stock, event-driven expiry restores stock + clears cart, checkout leaves no phantom restore, 409 on over-request, multi-item checkout totals correct. |
| 4 | Styling directive: use **Tailwind CSS v4** via `ng add tailwindcss`; casino palette as `@theme` tokens in `styles.css` (not scattered hex); utilities in templates, `@apply` only for 3+ repeats; responsive prefixes from the start; component CSS still fine for confetti keyframes. | Scaffolded Angular 18 standalone app; converted project SCSS→CSS; set up Tailwind v4 via `@tailwindcss/postcss` (the `ng add` schematic conflicted with Angular 18's peer deps). Defined the casino theme as `@theme` tokens and extracted `btn-gold`/`btn-ghost`/`slot-tile` `@utility` classes. Built services (api/socket/cart-state with BehaviorSubjects, auth interceptor) and all components (login, store-list, item-card w/ depleting stock pill, cart-drawer, checkout, order-confirmation w/ CSS coin-burst). |
| 5 | (continued) Phase 4 frontend verification + Phase 5 docs. | Drove the real UI headlessly (Playwright + Chrome): verified login→grab→cart→checkout→confirmation, that the cart drawer slides in, and that a second browser session sees a stock count drop live (10→9) with no reload and no console errors. Added graceful image fallback + item names on the confirmation. Wrote README.md (run instructions, API, env vars) and ARCHITECTURE.md (DDD scoping vs CQRS, Lua atomicity, event-driven expiry + restart tradeoff/reconciliation, no-NgRx, data model, Docker, TypeScript rationale). |
| 6 | Additive-only: add Swagger/OpenAPI, domain unit tests, integration tests (concurrency/expiry/checkout), and 2 Playwright E2E tests; wire scripts; update docs. No changes to routes/entities/use-case logic beyond minimal DI refactor. Plan first. | Gave a plan, confirmed, then implemented: (1) **Swagger** — `swagger-ui-express` + `swagger-jsdoc`, `@openapi` blocks on all 6 routes, component schemas in `openapi.ts`, served at `/api-docs`. (2) **Unit tests** — Jest ESM (ts-jest), 15 domain tests (Item/CartItem/Order/User), zero infra, ~0.5s. (3) **Integration** — Supertest vs real Docker Mongo/Redis: 20-concurrent-adds oversell guard (3 ok / 17× 409 / stock 0), event-driven expiry restores stock+clears cart, checkout clears reservation key (no double-credit). (4) **E2E** — standalone `e2e/` Playwright pkg (system Chrome, auto-boots both servers): happy path + live cross-client sync. (5) **Scripts** — `test` / `test:integration` (backend) + root `test:e2e`. Minimal production change: extracted `bootstrap()` from `server.ts` (no-listen + run-directly guard) so tests can import the app — noted in ARCHITECTURE.md §9. Updated README (Testing section, `/api-docs`) and ARCHITECTURE (testing strategy, bootstrap + e2e-placement rationale). All suites green. |
| 7 | Additive: auto-seed the catalogue on startup if Item collection is empty (Item only — not Cart/Order/User); extract shared seed data/logic reused by the CLI script; idempotent; update README/ARCHITECTURE. | Extracted `DEMO_ITEMS` + `seedItemsIfEmpty()` into `bootstrap/seedCatalogue.ts` (shared by CLI + startup). Wired the call into `bootstrap()` right after Mongo connect, before the Redis stock reconcile. Refactored `scripts/seed.ts` to reuse it (kept its destructive wipe + Redis refresh). Verified: empty catalogue → auto-seeds 6; restart → "catalogue present, skipping" with no duplicates (still 6); Cart/Order/User untouched. Unit (15) + integration (3) still green; CLI seed still works. README relegates `npm run seed` to optional; ARCHITECTURE §7 notes the Item-only idempotent auto-seed. |
| 8 | Additive: structured logging with pino/pino-http. Logger in `infrastructure/logging`, injected into use cases via DI (domain stays pure); request ids; log startup/cart-add/remove/checkout/expiry; socket broadcasts at debug; redact tokens; replace all console.*; docs + LOG_LEVEL. | Added `infrastructure/logging/logger.ts` (pino; pretty in dev, JSON otherwise; level from `LOG_LEVEL`; redacts `authorization`/`token`) and an application-layer `Logger` port. Injected the port into AddToCart/RemoveFromCart/Checkout/ExpireReservation via the composition root; added `pino-http` request logging (per-request id, skips /health + /api-docs). Logged the required events (startup: Mongo/Redis connected, seed check, stock reconcile, listening; cart add reserved/rejected; cart remove; checkout; reservation expiry) at info, socket `stock:update`/connect at debug, unhandled errors at error with route context. Replaced **all** console.* (verified none remain); added `NODE_ENV`/`LOG_LEVEL` to env config + `.env.example`. Verified: pretty dev output, prod raw JSON, LOG_LEVEL filtering, and that the auth token/header never leak (0 `authorization` in logs; the one userId occurrence is the intended checkout `userId` field). Unit (15) + integration (3) still green. README gains a Logging section; ARCHITECTURE notes pino-over-winston + the DI rationale. |
| 9 | Add increase/decrease of an item's quantity directly in the cart, through the existing atomic Redis path (new PATCH /cart/update, new UpdateCartItemQuantity use case, cart stepper UI, live stock, tests). | Backend: added `UpdateCartItemQuantity` use case — increase reserves `delta` via the existing `reserveStock` Lua (409 on insufficient), decrease releases `|delta|` and deletes row + reservation key at qty 0; resets reservation TTL on edit (new `refreshReservation` on the service); validates delta non-zero integer (400). Wired `PATCH /cart/update` (thin controller + route + OpenAPI). Frontend: `api.updateQuantity`, `cart.service.updateQuantity` with per-line 409 error state, and a gold `−/qty/+` stepper in the cart drawer (+ disabled at remaining 0, − at qty 1 does a full remove). Verified via curl (+/−/409/400, decrease-to-0 clears row+key) and a 2-client browser test (increase → other client sees SOLD OUT live, + disables, decrease restores, decrease-at-1 removes line, no console errors). Added 2 integration tests (20 concurrent increases → 3 ok/17× 409/stock 0; decrease-to-0 removes CartItem + reservation key) — full suite now 15 unit + 5 integration green. Docs: README API table + ARCHITECTURE concurrency note. |
| 10 | Add offset-based pagination to GET /items (page/limit envelope), paginate on the backend, update frontend + seed to ~30-40 items, tests + docs. | **Breaking API change** to `GET /items`: now `?page=1&limit=12` → envelope `{ items, page, limit, total, totalPages }`. Backend: added `findPage(page,limit)` to the `ItemRepository` interface (kept `findAll` for stock reconcile), Mongoose impl via `skip()/limit()` + `countDocuments()` (sorted by `_id`); `ListItems` now takes page/limit, validates positive ints (400), clamps `limit`≤100, and merges Redis `remaining` only for the page's items; controller passes query params. Expanded shared `DEMO_ITEMS` to **39** casino-themed items (low-stock ones scattered onto later pages). Frontend: `api.getItems(page,limit)`, `cart.service` page state (`pageState$`) + `goToPage`, and gold-themed prev/next + numbered pagination in store-list (active page highlighted); off-page `stock:update`s are ignored by the existing id-match patch. Verified: curl (pages/slices/clamp/400/out-of-range→empty 200) and browser (12/page, 4 pages, active indicator, Next disabled on last, live stock still patches on current page, no errors). Added 2 integration tests (page-2 slice+metadata; out-of-range→empty). Suites: 15 unit + 7 integration + 2 e2e all green. Docs: README API table + ARCHITECTURE pagination tradeoff note. |
