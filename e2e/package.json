{
  "name": "flashsale-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests (Playwright) driving the real frontend + backend",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
